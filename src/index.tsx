import { ToastProvider, useToast } from './ToastProvider';
import type {
  ToastInfo,
  ToastInfoWithId,
  ToastProviderProps,
  // CustomToastComponentProps,
} from './types';

export type { ToastInfo, ToastInfoWithId, ToastProviderProps };
export { ToastProvider, useToast };
